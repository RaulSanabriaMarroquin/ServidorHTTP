/*Define Config (por ahora solo port).

from_env_or_default() devuelve valores por defecto en Sprint 0.

Futuro: banderas tipo --workers.isprime=4, --queue.isprime=64, --timeout.cpu=60000, ruta data/, etc.
Todo lo que ajuste pooling, backpressure y timeouts nace aquí. */
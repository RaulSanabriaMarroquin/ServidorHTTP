/*
Estado mínimo de métricas:

Contador de conexiones (connections).

Timestamp de inicio (started).

Futuro:

Métricas por comando (colas, workers {total, busy}, latencias avg/std/p50/p95/p99).

Endpoint /metrics que exponga JSON.

/status (uptime, PID, conexiones atendidas, tamaño de colas). */